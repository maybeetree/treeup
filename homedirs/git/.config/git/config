# Generated by treeup. Do not edit!

#[include]
#	path=~/.config/git/config.d/10-aliases

[init]
	defaultBranch = main

# Origin is almost always github or gitlab,
# do not pollute it with annex branches
[remote "origin"]
	annex-sync = false

# Phone is usually offline
[remote "fp3.home"]
	annex-sync = false

[alias]
	tree = log --oneline --graph --decorate --all
	which = ls-files --others --exclude-standard
	# "recursive-annex-sync"
	ras = "!git submodule foreach --recursive 'git annex sync --commit ' && git annex sync --commit "
	# "recursive-annex-sync-content"
	rasc = "!git submodule foreach --recursive 'git annex sync --commit --content' && git annex sync --commit --content"
	# "recursive-annex-add-sync-content"
	raasc = "!git submodule foreach --recursive 'git add --all && git annex sync --commit --content' && git add --all && git annex sync --commit --content"
	# "recursive-annex-add-sync"
	raas = "!git submodule foreach --recursive 'git add --all && git annex sync --commit' && git add --all && git annex sync --commit"
	submods = "!find . -mindepth 1 -type d -exec test -d \"{}/.git\" \\; -exec git submodule add \"{}\" \"{}\" \\;"

